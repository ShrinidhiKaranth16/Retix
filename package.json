{
  "name": "@retix/common",
  "version": "1.0.0",
  "type": "module",       
  "main": "index.js",
  "types": "./build/index.d.ts",
  "files":[
    "build/**/*"
  ],
  "scripts": {
    "clean": "rimraf build",  
    "build": "npm run clean && tsc"
  },
  "devDependencies": {
    "rimraf": "^5.0.0"
  }
}
